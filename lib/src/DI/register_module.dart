import 'package:injectable/injectable.dart';

@module
abstract class RegisterSuperModule {
  // @Injectable(as: IHttpHelper)
  // HttpHelper httpHelper(IHttpConfig config) =>
  //     HttpHelper(config: config);

  // @Named('sessionManager')
  //  @Injectable(as: ISessionManager)
  //  SessionManager get sessionManager;

  // @Injectable(as: IFileUploadRepository)
  // FileUploadRepository fileUploadRepository(IHttpConfig config) =>
  //     FileUploadRepository(config: config);
  // @preResolve
  // FlutterSecureStorage get prefs => FlutterSecureStorage();

}
